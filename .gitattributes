# File default. This setting converts to LF line endings for the repository but
# allows user overrides when checking out into a working directory.
* text

# Binary files that should be ignored.
*.jpg binary
*.png binary 
